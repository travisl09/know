git push
CLIENT
    update README
    review css in view files for reusability/best practices
    review need for reverse proxy (is it just the client-side crypto?)
    exception handling in repos?
SERVER
    avoid using mutext in models.py?
    performance!