# CLIENT
    -   review css in view files for reusability/best practices
    -   review need for reverse proxy (is it just the client-side crypto?)
    -   exception handling in repos?
# SERVER
    -   make ollama model configurable
    -   make stable diffusion checkpoint configurable
    -   make model loading/unloading configurable or automatically turn on and off